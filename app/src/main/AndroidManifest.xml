<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.epl.uclouvain.uclove">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Page"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <activity
            android:name=".PageAcceuil"
            android:label="LoginDisplay"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".Menu"
            android:label="MenuDisplay"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".ProfilActivity"
            android:label="ProfilDisplay"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".FriendsActivity"
            android:label="FriendsDisplay"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".Requests"
            android:label="Requests"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".NewRequest"
            android:label="NewRequest">
        </activity>
        <activity
            android:name=".MyRequests"
            android:label="MyRequests">
        </activity>
    </application>

</manifest>
